query GetSpaceships {
  spaceships {
    data {
      _id
      name
    }
  }
}

query GetSpaceship($id: ID!) {
  findSpaceshipByID(id: $id) {
    _id
    name
  }
}

mutation CreateSpaceship($data: SpaceshipInput!) {
  createSpaceship(data: $data) {
    _id
    name
  }
}